name: Schedule Hello World

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  print-hello:
    runs-on: self-hosted
    steps:
      - name: Print hello
        run: echo "Hello World"
